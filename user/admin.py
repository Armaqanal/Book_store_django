from django.contrib import admin
from .models import Customer, Person, Writer

# Register your models here.
admin.site.register(Customer)
admin.site.register(Person)
admin.site.register(Writer)

# Register your models here.
